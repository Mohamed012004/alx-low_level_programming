#include "main.h"

/**
 * _abs - checks if input is litter.
 *
 * @n: check input of function
 *
 * Return: return -n. if n is negative
 * otherwise always n
*/

int _abs(int n)
{
	if ((n < 0)
		return (-n);
	return (n);
}
