#include <stdio.h>

/**
 * main - entry point
 *
 * Description: A C program to print all single
 * digit numbers of base 10 from 0 to 9.
 *
 * Return: 0 Always (success)
*/

int main(void)
{
	int digit = 48;

	while (digit <= 57)
	{
		putchar(digit)
		digit++;
	}
	putchar('\n');
	return (0);
}
